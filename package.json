{
  "name": "flaneur",
  "version": "1.0.0",
  "private": true,
  "description": "Flâneur — niche London detours, sorted by distance. Single-file React PWA; the spot catalogue lives in data/spots.json and is built into index.html.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "acorn": "^8.17.0"
  }
}
